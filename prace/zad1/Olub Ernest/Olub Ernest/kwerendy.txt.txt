1.	insert into rezerwacje values ("", "1", "2017-07-04", "4", "111222333");
2.	select `nazwa`, `cena` from `dania` WHERE typ=2;
3.	select `nazwa` from `dania` where (cena<15 && typ=3);
4.	update `pracownicy` set stanowisko=4 where stanowisko=3;